# DISABLED LEGACY FILE.
# The old Vast/30-job package must not be launched. The invalid provider makes
# direct `sky launch` fail during validation, before a paid resource is created.
resources:
  cloud: disabled_legacy_ayue_vast

run: |
  exit 64
